fastapi>=0.128,<1
uvicorn>=0.48,<1
sqlalchemy>=2.0,<3
pydantic>=2.13,<3
email-validator>=2.0,<3
cryptography>=46,<47
psutil>=7.0,<8
# Desktop legacy UI (optional during transition):
PySide6>=6.7,<7; platform_system == "Windows"
# Windows endpoint service / DPAPI (installed only on Windows packaging host)
pywin32>=306; platform_system == "Windows"
psycopg[binary]>=3.2,<4

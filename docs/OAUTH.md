# OAuth

OAuth providers are not yet activated. Production implementation must use provider-registered redirect URIs, state validation, PKCE where supported/appropriate, encrypted server-side token storage, refresh handling, revocation, least-privilege scopes and per-tenant connection ownership. OAuth tokens must never be injected into model prompts.

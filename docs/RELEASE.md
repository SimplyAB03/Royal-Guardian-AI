# Release Process

A release must pass unit/integration tests, type/static checks where configured, dependency/license review, security review, database migration verification and endpoint signature verification. Update `FEATURE_MATRIX.md` and `RELEASE_REPORT.md` for every commercial release. Never ship mocked critical actions as real functionality.

# API

Implemented core routes include health, registration/login/logout, current user, agent create/list/run history, integration status, audit events, device enrollment, heartbeat, device listing and device command queue/result reporting.

FastAPI exposes generated OpenAPI documentation at `/docs` while enabled by the runtime. Production deployments should decide whether public API docs remain exposed.
